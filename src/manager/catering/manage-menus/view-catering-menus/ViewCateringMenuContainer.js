import {connect} from 'react-redux'
import ViewCateringMenu from './ViewCateringMenu.js'

const mapStateToProps = (state, ownProps) => {
  return {

  };
};

const mapDispatchToProps = (dispatch, ownProps) => {
  return {

  };
};

const ViewCateringMenuContainer = connect(
  mapStateToProps,
  mapDispatchToProps
)(ViewCateringMenu);

export default ViewCateringMenuContainer;

