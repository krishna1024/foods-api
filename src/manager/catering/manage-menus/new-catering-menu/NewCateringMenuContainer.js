import {connect} from 'react-redux'
import NewCateringMenu from './NewCateringMenu.js'

const mapStateToProps = (state, ownProps) => {
  return {

  };
};

const mapDispatchToProps = (dispatch, ownProps) => {
  return {

  };
};

const NewCateringMenuContainer = connect(
  mapStateToProps,
  mapDispatchToProps
)(NewCateringMenu);

export default NewCateringMenuContainer;
