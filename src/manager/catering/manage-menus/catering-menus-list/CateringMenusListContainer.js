import {connect} from 'react-redux'
import CateringMenusList from './CateringMenusList.js'

const mapStateToProps = (state, ownProps) => {
  return {

  };
};

const mapDispatchToProps = (dispatch, ownProps) => {
  return {

  };
};

const CateringMenusListContainer = connect(
  mapStateToProps,
  mapDispatchToProps
)(CateringMenusList);

export default CateringMenusListContainer;

