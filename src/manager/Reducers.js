const reducers = {
    loadManager: function (state = {},action) {
        return state;
    },
    loadGoodManager: function (state = {},action) {
        return state;
    }
};

export default reducers
