const reducers = {
    loadPublic: function (state = {},action) {
        return state;
    },
    loadCustomer: function (state = {},action) {
        return state;
    }
};

export default reducers
